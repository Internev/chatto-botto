- Commence fires initial call to Claude to start conversation.
  - Init includes:
    - Create conversation in state.
      - Id
      - created(?)
      - Bot + User
    - Send initial claude message.
    - Append claude message to current conversation.
- Initialises a conversation queue in state that's sent each request.
- User responses are immediately added to queue and whole queue is sent. Response is subsequently added.
- On new response from bot, Polly speaks it aloud as soon as it arrives.

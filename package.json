{
  "name": "chatto-botto",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm:start:electron\" \"npm:start:web\" --kill-others --names \"electron,web\" --prefix-colors \"yellow.bold,cyan.bold\"",
    "start:electron": "npm run start --workspace=packages/electron",
    "start:web": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspaces --if-present"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "wait-on": "^7.2.0"
  }
}
